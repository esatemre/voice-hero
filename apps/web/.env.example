# Environment Variables Configuration
# 
# Development: Copy this file to .env.local and fill in the values
# Production: Set these variables in your hosting environment (Vercel, etc.)
# 
# Next.js automatically loads environment variables:
# - Development: Reads from .env.local at runtime
# - Production: Reads from server environment variables at runtime

# ============================================================================
# FIREBASE CONFIGURATION
# ============================================================================
# Obtain these from your Firebase Console > Project Settings > Web App Config
# https://console.firebase.google.com/

# Public Firebase Config (safe to expose in client-side code)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your-measurement-id

# Firebase Admin SDK (Server-side only - KEEP SECRET)
# Generate from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_CLIENT_EMAIL=your-service-account@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ============================================================================
# GOOGLE VERTEX AI / GEMINI
# ============================================================================
# Configured through Firebase Service Account with Vertex AI API enabled
# Set GOOGLE_APPLICATION_CREDENTIALS environment variable in production
# API will use FIREBASE_CLIENT_EMAIL and FIREBASE_PRIVATE_KEY above

# ============================================================================
# ELEVENLABS CONFIGURATION
# ============================================================================
# Get your API key from: https://elevenlabs.io/app/voice-lab
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# URL where the app is hosted (used for redirects, widget integration, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# FEATURE FLAGS (Firebase Remote Config)
# ============================================================================
# Feature flags are managed in Firebase Remote Config (runtime configurable).
# Suggested parameters:
# - email_onboarding_enabled (boolean)
# - new_project_creation_enabled (boolean)

# SMTP2GO configuration (required when email_onboarding_enabled is true)
SMTP2GO_API_KEY=your-smtp2go-api-key
SMTP2GO_SENDER=VoiceHero <hello@yourdomain.com>
SMTP2GO_REPLY_TO=support@yourdomain.com

# ============================================================================
# DEPLOYMENT CONFIGURATION (Optional)
# ============================================================================
# Uncomment and set for production deployments
# NODE_ENV=production
